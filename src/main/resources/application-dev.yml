spring:
  application:
    name: google_lms

  datasource:
    url: jdbc:postgresql://localhost:5444/shop_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6333
      database: 0

  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    enabled: true

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: false

  banner:
    location: banner/banner-dev.txt

      #  mail:
      #   enable : true
      #  host: smtp.gmail.com
      # port: 587
      #username: abdunabiyevbexruz09@gmail.com
      #    password:
      #   properties:
      #    mail:
      #     smtp:
      #      auth: true
      #     starttls:
    #     enable: true

springdoc:
  swagger-ui:
    path: /docs
    enabled: true
    doc-expansion: none
    persist-authorization: true
  api-docs:
    path: /v3/api-docs
    enabled: true

jwt:
  secret: nfewongib43ht3096ierj0iggrenogn59uy6835u04u285y
  expiration: 360000